<resources>

    <!-- Base application theme. -->
    <style name="app_theme" parent="@style/Theme.AppCompat" >

        <item name="android:colorPrimaryDark">@color/status_bar</item>
        <item name="android:colorPrimary">@color/primary_material_dark</item>
    </style>

</resources>
